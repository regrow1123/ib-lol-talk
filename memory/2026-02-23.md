# 2026-02-23

## Full Code Rewrite Complete
- Rewrote all code from scratch based on PRD + TDD
- Server: champions.js, game.js, damage.js, validate.js, prompt.js, llm.js
- API: turn.js, skillup.js (no /api/start)
- Frontend: KakaoTalk-style chat UI, setup screen, client state init
- Commit: `4e964bb`

## Vercel Deployment Issues Fixed
- 404 on /api/*: old builds/routes config conflicted with Vercel auto-detection → simplified to `functions` + `rewrites`
- Champion JSON loading: `readFileSync` failed in serverless → switched to `createRequire`
- `includeFiles: "data/**"` in vercel.json functions config

## UI Fixes
- Mobile layout: 100dvh, position:fixed, safe-area-inset-bottom
- Rune icons: each rune tree has different DDragon path (Precision/Domination/Resolve)
- Skill icons: DDragon uses `LeeSinQOne` not `LeeSinQ`
- All icons downloaded to `src/img/` locally (no more CDN dependency)
- Champion portrait added to enemy chat bubbles
- Resource bar added under HP bar

## Prompt Improvements
- Merged old Korean prompt strengths with new architecture
- Old prompt had better quality: Korean instructions, explicit combat rules, interrupt mechanics
- aiChat must speak as OPPONENT (enemy player perspective), not narrator
- Key rules: no mercy, varied strategies, interrupt, intensity-based elapsed

## ifLevelUp Flow (PRD Design)
- No extra API call needed after skillup
- LLM generates ifLevelUp suggestions during turn (knows CS→Level table)
- Client re-filters stored suggestions with chosen skill
- Removed unnecessary levelUpHint system (LLM decides CS, knows level-up itself)
- Removed /api/suggestions endpoint (was wrong approach)

## Architecture Notes
- Vercel auto-detects api/ folder for serverless functions
- Don't use custom builds/routes — conflicts with auto-detection
- createRequire works across Node 18+ for JSON imports in ESM
